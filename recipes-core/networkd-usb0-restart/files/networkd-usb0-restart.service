[Unit]
Description=Restart networkd after boot for usb0
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/sleep 10
ExecStart=/bin/systemctl restart systemd-networkd

[Install]
WantedBy=multi-user.target
